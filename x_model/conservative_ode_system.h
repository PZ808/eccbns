double dphi_dt(
		double u,
		double eta,
		double e,
		double x );
double separation(
		double u,
		double eta,
		double e,
		double x );
double mean_motion_func(
		double e,
		double eta,
		double x );
double angular_eccentricity(
		double u,
		double eta,
		double e,
		double x );
#if 0
double mean_anomaly_func(
		double u,
		double e,
		double eta,
		double x );
#endif

